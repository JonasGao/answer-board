# CONTEXT

本项目的领域术语表（glossary）。只记术语与概念，不记实现细节。

## 术语

- **Grilling 会话（Grilling Session）**：一个 agent 针对某个主题持续进行的独立盘问会话，由稳定的「会话标识」区分，可有专用的展示名称。多个会话可同时存在，彼此的轮次、问题和回答不混合。
- **本地会话（Local Session）**：答题板启动时存在的默认会话，用于不通过外部投递的手工作答流程。
- **轮次（Round）**：某个 Grilling 会话中一次投递的全部问题，也是用户当次集中作答的边界。每个会话只展示当前轮；新轮先作为待接收轮，待用户明确进入后取代当前轮。
- **待接收轮（Pending Round）**：已经投递到会话、但尚未取代当前轮的一组问题。每个会话最多保留一个待接收轮，用户进入后它成为当前轮。
- **问题条目（Question Entry）**：轮次中的一道问题，由「编号」、「问题」、「推荐回答」和用户的「回答」组成。
- **问题（Question）**：Grilling agent 要用户作出回应的具体提问，可包含标题、说明和备选项。避免用「问题」指代整个条目。
- **推荐回答（Recommended Answer）**：Grilling agent 随问题给出的建议答案，供用户作答时参考，不是用户的回答。
- **回答（Answer）**：用户针对问题写下的多行正文，与推荐回答明确区分。
- **编号（Number）**：问题条目在所属轮次内的必填标识（Q1、Q2…），随条目存储，不从位置派生。板内编号唯一，条目按编号升序排列；编号不必连续，缺口保留，删除条目不影响其余编号。
- **下一个编号（Next Number）**：派生值：当前板上最大编号 + 1；板为空时为 1。重编号输入框的默认预填值，也是「添加」分配给新条目的编号。
- **重编号（Renumber）**：唯一的清板重建操作：输入编号列表（正整数，逗号/空格分隔）→ 清空所有条目 → 按编号升序逐条重建；输入含重复编号时整次拒绝。快捷键 Alt+Enter。输入框默认预填「下一个编号」。列表只含一个数时即旧语义：清空后从该编号建一条。
- **改号（Relabel）**：单个条目的就地改号：点击编号标签输入新编号，条目按新编号落位。新编号与板内已有编号相同时拒绝。用于轮次中途补答旧编号。
- **界面字体（UI Font）**：界面自身（header、按钮、对话框等非条目部分）使用的字体。用户口语中的「界面自己」即界面字体。
- **回答输入框字体（Answer Input Font）**：回答输入框使用的字体。用户口语中的「答题输入框字体」即回答输入框字体。
- **编号字体（Number Font）**：编号标签（Q1、Q2…）使用的字体。用户口语中的「题目编号字体」即编号字体。
- **字体配置（Font Settings）**：集中查看和修改上述三种字体的界面，入口在 header 的「Fonts…」。
